# Enhancements
- Multi-edit now supports various create options for properties form the "gear" menu in column header. 
- Machine list "Id" column renamed to "machine id" since the table contains many different types of item entities. 
- Improve wildcards for lazy data models. Allow ? and *. 
# Traveler
- List travelers button from machine templates is now functional. Will list from items created from template. 
- Resolve issue with traveler title still small when in Firefox.
- Resolve issue with latest version not showing for traveler version selection. Now showing all versions from newest to oldest. 
# Cable and Import
- fix scroll and layout issues in cable wizard for manually creating cables
- allow updating machine item parent from machine item update import spreadsheet
- allow setting inventory installed status from machine item create and update import spreadsheets
- cable preimport script error message cleanup, handling for cable type catalog items that already exist in CDB, and config flag for ignoring the "from/to port" columns on the Cables tab of the kabel workbook
# Bug fixes
- Resolve issue with updating installed flag via API for active machine installations. 
- Resolve possible null exception when downloading legacy files without a "display value" set. 
- Add more specific mime-type to resolve the proxy header of "nosniff" causing downloading issues. 
# Other
- Resolve issues with metadata-extractor library. 
- Resolve possible foreign key issue during database restore. 
