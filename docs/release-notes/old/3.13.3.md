# Enhancements
- Allow saving of machine design filter configuration including the filter box and machine filter selections. 
- Functions can be listed for cable catalog components. 
- Allow use of sequence to specify format for the sequence (example 001 will format the number with two leading zeros). 
# Bug Fixes
- PDF documents now open in new tab for document upload and image property types. 
- Resolve issues with creating travelers from multi-edit for cable inventory items. 
- Resolve issues with updating status from multi-edit for cable inventory items. 
- Resolve issue with fetching status from cable inventory items via API.  
- Allow updating of location details for all machine designs (don't prevent based on membership). 
- Resolve issues with not enforcing a location details limit to 64 characters. 
- Resolve issue with shown empty gallery preventing addition of properties to items. 
- Resolve issue with jump to qrid with error blocking the UI from performing another request (also prevent invalid typed characters). 
# Other
- Remove deprecated & unused domain 'cable'. 
