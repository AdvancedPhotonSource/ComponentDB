# General 
- Allow machine designs to have QRIDs
# Location Enhancements 
- Add support for apply location and location details to all in multi-edit screen.
- Reverse location single node hierarchy on item details page to make it easier to read for user
- Machine design location can now be assigned regardless of membership to another machine desing. 
- Machine design single node location hierarchy shows a combination of machine design and locations when applicable. 
# Item Element Enhancements
- Use the referenced catalog item element name for inventory element names as source of truth. 
- Add a column for part name and settings associated with it.
- Prompt users with enabled part name column to specify part name when adding an item to assembly listing.
# Bug Fixes
- Resolve missing data to update location tree with current location when setting location using dialog for inventory.
- Prevent deletion of users associated with items. 
