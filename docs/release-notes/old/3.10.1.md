# Enhancements 
- Add mechanism for setting owner user/group for machines hierarchically during creation. 
- Add mechanism for setting owner user/group for all domains during import.
- Allow importing of machine inventory.
- Allow importing of location. 
# API
- Allow API to clear a contained item from an element in an assembly. 
- Ability to fetch item memberships for items. 
# Bug Fixes
- Resolve issue with display of qr id on machine list page. (default: not shown)
- Resolve issue with location and location history not showing on inventory when assigned to machine 
- Resolve missing expand node on locations list page
- Resolve error 500 on cable details page
- Resolve issue with filter by item sources in catalog and cable catalog
# Other
- "Get it on Google Play" badge on about page. 
- Update Jackson libraries 
- Clean up some of the fields returned via API
