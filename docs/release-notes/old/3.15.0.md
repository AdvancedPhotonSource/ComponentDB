# Control Hierarchy Enhancements
- Allow a control node to be controlled by multiple parents. 
  - Add circular reference protection. 
  - Search results in multiple branches expanded when multiple parent exists at any level of the result hierarchy. 
  - Linked Parent Functionality.
    - Support reference to link a child to a specific parents parent. 
    - Add reference to database
    - Update stored procedures for optimized parent/child fetching with linked parent. 
    - Fetch correct children in control hierarchy tree table. 
    - Fetch correct parents for control hierarchy tree table search results. 
# Machine Design
- Represented assembly part after creation of machine. (Also known as promoted machine element). 
  - Allow user to link to a specific part for a machine. 
  - Allow user to link to a specific part for machine template, which will update all the instances. 
    - Support match by element name to provide support for instances with `custom assemblies. 
  - Allow clearing of represented assembly part.  
- Add maximum machine result nodes setting to allow user to display more machine search results. 
# API
- Fetch a list of all control hierarchies that a node is currently a part of. 
- Fetch correct control hierarchies based on the link parent functionality. 
- Allow user to update the represented assembly part for machine element. 
# CLI
- Add housing hierarchy to cdbInfo. 
- Display all of the control hierarchies.  
- Other enhancements. 
# Bug Fixes
- Resolve issue with that allows user to remove item that aren't in the trash can. 
  - Provide additional protection to ensure valid deletion such as validating that item is already of type "deleted" 
  - Do not allow editing with machine with simple view only the specific views for it. 
- Resolve issue with cloning targetValue and displayValue of property values. 
- Resolve filtering of certain fields for cable design
- Resolve export for certain MAARC items with excel data. 
- Resolve issue with deleting MAARC items. 
- Resolve jump to non-existent qrid or issue with opening inventory create pages. 
- Resolve issue with ignored sort order for item element list by presorting the list due to sortBy not working on new primefaces. 
- Resolve json format non-compliance for cdb-cli
- Resolve issue with wildcard searches for control hierarchy. 
- Resolve issue with adding a placeholder to machine template with instances (also use updateList to fail all if one fails). 
- Remove non-supported "Add" options for machine templates. 
