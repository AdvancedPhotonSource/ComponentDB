# Location
- Support relocation of locations (update location parent).
# Inventory 
- Implement a lazy data table framework for inventory items (extendable to MAARC with plans for rest domains in future release) 
# MAARC
- Utilize the newly updated lazy data table framework (Adds sorting. additional filters and stability to the previously domain specific implementation)  
# Machine
- eTraveler Template selection is derived from template created from or catalog of assigned item to machine.
- Add trash can feature with move to trash, restore, and permanently delete operations and trash can list view.
# API
- Add ability to update location parent via APIs. 
- Add ability to fetch location types for the purpose of assigning location type.
- Add API for finding machine element by name or alternate name in specified rack/enclosure.
- Add API to fetch system logs.
- Add API for fetching and updating permissions.
- Add API for fetching all user groups and by name. 
- Add sort order for item hierarchy 
- Resolve swagger openAPI standard invalid.
- Improve API path consistency (add deprecated paths) 
- Seems that only admins had permissions to items, now standard permission check applies. 
# Import Framework
- Add support for sort order for locations, machine hierarchy, and catalog assemblies.
# Cable Pre-Import
- Handle new cable catalog procurement columns and cable design routing columns.
- Add framework for Kabel workbook validation.
# Bug fixes
- Resolve machine inventory add top level to all user to add top level inventory. 
- Resolve machine inventory permalink issue (internal server error) 
- Resolve issue where wrong top level items show up for add top level for machine inventory. 
- Resolve filtering issues (item identifier 1 & 2) 
# Other
- Update Swagger related libraries 
