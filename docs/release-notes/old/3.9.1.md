# Bug fixes
- Resolved issue with toggling favorites when none exist.
- Resolve issue with listing multiple favorite top level parents when multiple children are in favorites. 
- Resolved issue with missing js for machine design templates.
- Disabled drag and drop for machine design templates.
- Resolved null exception when fetching a location for certain inventory items using web service. 
- Renamed the API call for getItemByIdFromAPI back to getItembyId. 
