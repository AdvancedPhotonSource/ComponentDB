# API Enhancements
- Item Project
  - Allow user to fetch all item projects
- Property Value
  - Allow user to fetch property value by id
  - Allow user to fetch property value history 
  - Allow user to fetch property value metadata
- Domain
  - Allow user to fetch domain by id
  - Allow user to fetch relevant categories and types per domain
  - Allow user to fetch categories and types by id
  - Allow user to fetch relevant types for a category 
  - Allow user to fetch relevant categories for a type
